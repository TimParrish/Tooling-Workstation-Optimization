### Vim config file

Vim has many configuration options, but some basic options that I have set up are:

- Basic syntax highlighting
- Show the line numbers in the file
- Set the 'tab' to be 4 spaces vs. a hard tab
- Show a line at the 80-character mark
